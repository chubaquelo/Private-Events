<%= form_with model: @session, url: session_index_path do |f| %>
  <%= f.label :name, 'Enter your name:', class: 'lead' %><br>
  <%= f.text_field :name, class: "mb-4 form-control mx-auto w-25 border border-dark" %><br>
  <%= f.submit 'Login', class: "btn btn-success" %>
<% end %>
